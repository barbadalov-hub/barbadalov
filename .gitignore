# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock

# Machine-specific / generated platform files
**/local.properties
.gradle/
**/ephemeral/
**/Flutter/flutter_export_environment.sh
**/GeneratedPluginRegistrant.*

# Android signing (never commit keystores/secrets)
**/*.keystore
**/*.jks
**/key.properties

# IDE
.idea/
.vscode/
*.iml

# Firebase secrets (generated by `flutterfire configure`)
lib/firebase_options.dart
google-services.json
GoogleService-Info.plist
firebase.json
.firebaserc

# OS
.DS_Store
Thumbs.db

# Firebase build-time config (your project ids) — keep the .example, ignore the real one
firebase.defines.json
