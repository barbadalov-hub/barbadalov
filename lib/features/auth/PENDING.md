# auth — scaffolded (Phase 3)

Firebase Auth (email + Google/Apple sign-in). Emits `user_signed_in` /
`user_signed_out` events. Until this ships, MoneyOS uses a local `userId`.

Planned layout: `domain/ application/ data/ presentation/` mirroring `money/`.
